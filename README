Seq-ACL Countermodels: A Decidable Theorem Prover for ACL with Countermodels generation

To lunch Seq-ACL Countermodels

1. Run SWI-Prolog ($ swipl)
2. Load latexify package (?- [latexify].)

Now you can prove formulas by typing

?- prove(a->a).

Or you can run test files as follows

?- ['examples/tests.pl'].
